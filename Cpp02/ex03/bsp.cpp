#include "Point.hpp"

bool    bsp(Point const a, Point const b, Point const c, Point const point)
{
    int height;
    int length;

    if (a.getX().getRawBits() >= b.getX().getRawBits())
        length = Fixed::max(a,c).x.getRawbi;

}